#include <stdio.h>

#define __FLAG__ "sigpwny{big_preload_energy}"

int find_me_you_wont (void) {
	for (int i = 0; i < 1; i++) {
		fprintf(stdout, "WINNER WINNER!!! %s\n", __FLAG__);
	}
}

int main () {
	puts("Hey kids");
	return 0;
}
